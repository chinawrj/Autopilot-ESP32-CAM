# Autopilot ESP32-CAM — 实时摄像头 Web 服务器
# ESP-IDF v5.x 项目

cmake_minimum_required(VERSION 3.16)

set(PROJECT_VER "1.1.0")

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(autopilot-esp32-cam)
