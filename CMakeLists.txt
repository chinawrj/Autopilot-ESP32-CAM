# Autopilot ESP32-CAM — Real-time Camera Web Server
# ESP-IDF v5.x Project

cmake_minimum_required(VERSION 3.16)

set(PROJECT_VER "2.0.0")

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(autopilot-esp32-cam)
